syntax = "proto3";

package api;

service MCPService {
  rpc CallTool (ToolRequest) returns (ToolResponse);
}

message ToolRequest {
  string toolName = 1;
  string argumentsJson = 2;
}

message ToolResponse {
  string resultJson = 1;
  bool isError = 2;
}